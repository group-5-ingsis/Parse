spring:
  application:
    name: "Parse Service"
  data:
    redis:
      host: localhost
      port: 6379

stream:
  format: ${STREAM_KEY_FORMAT}
  validate: ${STREAM_KEY_VALIDATE}

groups:
  parser: ${GROUP_ID}

server:
  port: ${PARSE_SEVER_PORT}
