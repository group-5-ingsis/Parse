package com.ingsis.parse.parser

import org.springframework.stereotype.Service

@Service
class ParseService {

  fun formatSnippet(snippetId: String) { }

  fun lintSnippet(snippetId: String) { }

  fun formatAllSnippets(snippetId: String) { }

  fun lintAllSnippets(snippetId: String) { }
}
